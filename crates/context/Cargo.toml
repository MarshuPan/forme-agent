[package]
name = "forme-context"
version = "0.0.1"
edition.workspace = true
publish.workspace = true

[dependencies]
forme-protocol = { path = "../protocol" }
forme-store = { path = "../store" }
forme-memory = { path = "../memory" }
